package com.g10.gauchogrub.io;

import java.net.URL;

public class WebUtils {

    /* Downloads a file from a web URL */
    public String downloadFile(URL url, int timeout){
        // Might require slight signature change
        // Will probably require private helpers
        return null;
    }
}